# bb

CLI entry point package for the open-source `blackbox` commands.

If you are only using `blackbox-pro`, this folder is not required.

Common commands:
```bash
blackbox --root ./.blackbox_store list --project acme --dataset demo
blackbox --root ./.blackbox_store report --project acme --dataset demo --run-id <id>
```
